numpy>=1.21
pypylon>=4.0

# OpenCV is expected to be installed on the Jetson system.
# For CSRT/KCF/MOSSE trackers, OpenCV must include the tracking/contrib module.
#
# Basler pylon must be installed on the Jetson system before pypylon is used.
